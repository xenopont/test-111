import { ipButtonClassName } from './ip-button/style';

const IpButton = () => {
    return (
        <button type="button" className={ipButtonClassName}>My IP Address</button>
    );
}

export default IpButton;
